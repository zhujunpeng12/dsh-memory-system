# evidence-ledger bundle layer: mount the tool-call ledger plugin (index.js via
# the package root export). Zero inject — it only listens to session/event.
# Ledger location: ${DSH_HOME}/storages/tool-telemetry.json (host-level, spans
# workspaces, never resets).
- insert:
    - id: evidence-ledger
      name: 'evidence-ledger'
