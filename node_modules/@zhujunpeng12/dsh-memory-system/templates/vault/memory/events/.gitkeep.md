# 事件日志目录

每天一个文件：`YYYY-MM-DD.md`。格式：

```
# 2026-08-15

## 主标题（≤180 字节，会被 bootstrap 热包索引）

**内容：** 当天做了什么、结论是什么。
**核验：** 怎么验证的。
```

原始轨迹（session 日志、原始流水）用 `YYYY-MM-DD.raw.md` 后缀，默认被召回排除；提炼后的结论才进 `YYYY-MM-DD.md`。
