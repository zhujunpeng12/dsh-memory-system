# 规则（示例）

> 复制为 `rules.md`。格式：`<编号>. ⏱×<引用次数> <正文>`，`⭐` 标记核心规则。
> `rules-core.md` 由 `sync-core.py` 按 ⭐ / 引用次数自动生成。

## 示例节

1. ⏱×1 ⭐ 示例规则：任何仓位变更都要先核对最大回撤。**Why:** 这是示例格式，改成你自己的规则。
2. ⏱×0 第二条规则：写文件必须显式 `encoding="utf-8"`。
